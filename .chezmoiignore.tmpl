install.sh

{{- /* known_hosts drifts constantly (SDM rotates ports); let SSH manage it. */}}
.ssh/known_hosts
.ssh/known_hosts.old

{{- /* Pi config: sync stable config/code, not credentials, sessions, binaries, packages, or runtime state. */}}
.pi/agent/auth.json
.pi/agent/sessions
.pi/agent/sessions/**
.pi/agent/bin
.pi/agent/bin/**
.pi/agent/npm
.pi/agent/npm/**
.pi/agent/git
.pi/agent/git/**
.pi/agent/agentmemory.log
.pi/agent/agentmemory.pid

{{- /* VS Code config: apply OS-appropriate path, ignore the other. */}}
{{ if ne .chezmoi.os "darwin" -}}
Library
Library/**
{{ end -}}
{{ if eq .chezmoi.os "darwin" -}}
.config/Code
.config/Code/**
{{ end -}}
